<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="BasicDefinitions.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="RightExpander" TargetType="Expander">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentPresenter Content="{TemplateBinding Content}">
                        <ContentPresenter.LayoutTransform>
                            <RotateTransform Angle="-90"/>
                        </ContentPresenter.LayoutTransform>
                    </ContentPresenter>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        
        <Style.Triggers>
            <Trigger Property="IsExpanded" Value="True">
                <Setter Property="Expander.Width"
                        Value="{Binding RelativeSource={RelativeSource Self}, Path=MaxWidth}"/>
            </Trigger>
            <Trigger Property="IsExpanded" Value="False">
                <Setter Property="Expander.Width" Value="{StaticResource CollapsedWidth}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>